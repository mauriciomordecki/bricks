import 'package:flutter/material.dart';

class {{name.pascalCase()}}TextField extends StatelessWidget {
  const {{name.pascalCase()}}TextField({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const TextField();
  }
}
